Use the `nbs-pr-reviewer` agent (subagent_type: nbs-pr-reviewer) to review the pull request at $ARGUMENTS.

Pass `$ARGUMENTS` as the PR URL directly to the agent.

The agent will present findings for approval before posting any comments to the PR.
